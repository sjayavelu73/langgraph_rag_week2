# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Override default model
# OPENAI_MODEL=gpt-4o
# OPENAI_TEMPERATURE=0

# PDF Processing Configuration
# Optional: Specify PDF file paths (comma-separated)
# If not set, will look for PDFs in the data/ directory
# PDF_FILE_PATHS=/path/to/file1.pdf,/path/to/file2.pdf

# Optional: Override default chunking settings
# CHUNK_SIZE=800
# CHUNK_OVERLAP=200

# Vector Database Configuration
# Optional: Override default collection name
# COLLECTION_NAME=RAG5

# Retrieval Configuration
# Optional: Override default retrieval count
# RETRIEVAL_K=10
